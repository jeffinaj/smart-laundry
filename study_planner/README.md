# Study Planner (Django)

This is a full-stack Study Planner built with Django, MySQL, HTML/CSS/JS.

Setup and run instructions are in the workspace README. Configure MySQL credentials in environment variables or in `study_planner/settings.py`.
