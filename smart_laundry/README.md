# Smart Laundry Management

This repository contains a full-stack Smart Laundry Management application built with React, Django, and MySQL.

- `backend/` contains the Django REST backend.
- `frontend/` contains the React user interface.

Follow the setup instructions in `SETUP.md`.
